# ============================================
# COMMUNICATION TRIAGE SERVER CONFIGURATION
# ============================================
# Copy this file to .env and fill in your values

# Server
PORT=3000

# ============================================
# SUPABASE (Required)
# ============================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# ============================================
# GMAIL INTEGRATION
# ============================================
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a new project or select existing
# 3. Enable Gmail API
# 4. Go to "Credentials" > "Create Credentials" > "OAuth Client ID"
# 5. Choose "Web application"
# 6. Add http://localhost:3000/auth/google/callback as authorized redirect URI
# 7. Copy the Client ID and Client Secret here

GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-client-secret

# After running the server and visiting /auth/google, you'll get this:
GMAIL_REFRESH_TOKEN=

# For push notifications (optional but recommended):
# 1. Create a Pub/Sub topic in Google Cloud Console
# 2. Grant "gmail-api-push@system.gserviceaccount.com" publish permissions
# 3. Create a push subscription pointing to your /gmail/webhook endpoint
GMAIL_PUBSUB_TOPIC=projects/your-project/topics/gmail-notifications

# ============================================
# SLACK INTEGRATION
# ============================================
# 1. Go to https://api.slack.com/apps
# 2. Create a new app (or use existing)
# 3. Go to "Basic Information" to get Signing Secret
# 4. Go to "OAuth & Permissions" to get Bot Token
# 5. Add these bot scopes: users:read, im:history, im:read
# 6. Install app to workspace
# 7. Go to "Event Subscriptions", enable events
# 8. Set request URL to: https://your-server.com/slack/webhook
# 9. Subscribe to bot event: message.im

SLACK_SIGNING_SECRET=your-signing-secret
SLACK_BOT_TOKEN=xoxb-your-bot-token
